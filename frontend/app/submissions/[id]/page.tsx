"use client";

import Link from "next/link";
import { useEffect, useState } from "react";

const API = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000/api";

export default function SubmissionPage({ params }: { params: Promise<{ id: string }> }) {
  const [submission, setSubmission] = useState<any>(null);
  const [selected, setSelected] = useState<any>(null);
  const [error, setError] = useState("");
  const [overrideMarks, setOverrideMarks] = useState("");
  const [overrideReason, setOverrideReason] = useState("");
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    params.then(({ id }) => fetch(`${API}/submissions/${id}`).then((response) => response.ok ? response.json() : Promise.reject()).then((data) => { setSubmission(data); setSelected(data.evaluations[0] ?? null); }).catch(() => setError("This submission could not be loaded.")));
  }, [params]);

  if (error) return <main className="min-h-screen bg-[#f5f1e9] p-8 text-[#172126]">{error}</main>;
  if (!submission) return <main className="min-h-screen bg-[#f5f1e9] p-8 text-[#566164]">Loading assessment...</main>;
  const answer = submission.answers?.find((item: any) => item.question_id === selected?.question_id);
  async function saveOverride(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (!selected || !overrideMarks) return; setSaving(true); const response = await fetch(`${API}/evaluations/${selected.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ marks: Number(overrideMarks), reason: overrideReason || null }) }); if (response.ok) { const refreshed = await fetch(`${API}/submissions/${submission.id}`); const data = await refreshed.json(); setSubmission(data); setSelected(data.evaluations.find((item: any) => item.id === selected.id)); setOverrideMarks(""); setOverrideReason(""); } setSaving(false); }
  return <main className="min-h-screen bg-[#f5f1e9] text-[#172126]"><header className="border-b border-[#172126]/10 bg-[#fcfaf5] px-5 py-4 sm:px-8"><div className="mx-auto flex max-w-7xl items-center justify-between"><Link href="/" className="font-serif text-2xl font-bold">PRISM</Link><span className="text-sm text-[#566164]">{submission.student_name} · {submission.exam_title}</span></div></header><div className="mx-auto grid max-w-7xl gap-6 px-5 py-6 xl:grid-cols-[minmax(0,1.25fr)_minmax(360px,0.75fr)]"><section className="rounded-lg border border-[#172126]/10 bg-[#fcfaf5] p-4"><div className="mb-4 flex items-center justify-between"><h1 className="font-serif text-xl font-semibold">Original paper</h1><span className="text-sm text-[#667174]">{submission.pages.length} page{submission.pages.length === 1 ? "" : "s"}</span></div>{submission.pages.length ? <div className="space-y-4">{submission.pages.map((page: any) => <img key={page.id} src={`${API.replace(/\/api$/, "")}${page.url}`} alt={`Original paper page ${page.page_number}`} className="w-full rounded-md bg-[#f5f1e9] outline outline-1 outline-[#172126]/10" />)}</div> : <p className="rounded-md bg-[#f5f1e9] p-4 text-sm text-[#667174]">No original paper is attached to this cached submission.</p>}</section><aside className="min-w-0"><section className="rounded-lg border border-[#172126]/10 bg-[#fcfaf5]"><div className="border-b border-[#172126]/10 p-5"><p className="text-sm text-[#667174]">Assessment result</p><h2 className="font-serif text-2xl font-semibold">{submission.total_score} marks</h2></div><div className="max-h-[420px] divide-y divide-[#172126]/8 overflow-y-auto">{submission.evaluations.map((evaluation: any) => <button type="button" key={evaluation.id} onClick={() => setSelected(evaluation)} className={`w-full p-4 text-left ${selected?.id === evaluation.id ? "bg-[#173f4c]/6" : "hover:bg-[#173f4c]/3"}`}><div className="flex justify-between gap-3"><span><strong className="block text-sm">{evaluation.question_number} · {evaluation.criterion_title}</strong><span className="text-xs uppercase tracking-wide text-[#667174]">{evaluation.concept}</span></span><strong className="font-mono text-sm">{evaluation.effective_marks}/{evaluation.max_marks}</strong></div></button>)}</div></section>{selected && <section className="mt-5 rounded-lg border border-[#172126]/10 bg-[#fcfaf5] p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-sm text-[#667174]">{selected.question_number}</p><h2 className="font-serif text-xl font-semibold">{selected.criterion_title}</h2></div><span className={selected.needs_review ? "text-xs font-medium text-[#a15130]" : "text-xs font-medium text-[#52705b]"}>{selected.needs_review ? "Review recommended" : "High confidence"}</span></div><p className="mt-4 text-sm leading-6 text-[#566164]">{selected.reason}</p><blockquote className="mt-4 border-l-2 border-[#c29b54] pl-3 text-sm italic text-[#566164]">“{selected.evidence[0]?.quote ?? "No quotation was returned."}”</blockquote>{answer && <div className="mt-5 border-t border-[#172126]/10 pt-4"><h3 className="text-sm font-medium">Transcription</h3><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-[#566164]">{answer.transcription}</p>{answer.uncertainty.length > 0 && <p className="mt-3 text-xs text-[#a15130]">Contains uncertain transcription.</p>}</div>}<form onSubmit={saveOverride} className="mt-5 border-t border-[#172126]/10 pt-4"><h3 className="text-sm font-medium">Teacher override</h3><div className="mt-3 grid gap-2 sm:grid-cols-[100px_1fr_auto]"><input aria-label="Override marks" className="input" type="number" min="0" max={selected.max_marks} step="0.5" value={overrideMarks} onChange={(event) => setOverrideMarks(event.target.value)} placeholder="Marks" /><input aria-label="Override reason" className="input" value={overrideReason} onChange={(event) => setOverrideReason(event.target.value)} placeholder="Optional reason" /><button type="submit" disabled={saving} className="rounded-md bg-[#173f4c] px-3 py-2 text-sm font-medium text-white">{saving ? "Saving" : "Apply"}</button></div></form></section>}</aside></div></main>;
}
